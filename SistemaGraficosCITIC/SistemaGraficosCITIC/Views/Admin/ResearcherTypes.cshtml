@model List<SistemaGraficosCITIC.Models.Domain.ResearcherTypes>

@{
    ViewData["Title"] = "Tipos de publicación";
}

<div>
    <h4>Tipos de Investigador</h4>
    <hr />
    <button type="button" class="btn btn-primary m-2" onclick="openTheForm()">
        <b>+</b> Agregar Tipo
    </button>
    <!-- The form -->
    <div class="form-popup" id="popupForm" style="display: none">
        <form asp-action="ResearcherTypes" class="form-container" id="addTypeForm">
            <input class="form-control-sm " type="text" placeholder="Nuevo nombre de tipo" name="ResearcherTypeName" required>
            <button type="submit" class="btn btn-outline-primary">Agregar</button>
            <button type="button" class="btn btn-close" onclick="closeTheForm()"></button>
        </form>
    </div>

    <table class="table table-borderless text-left" id="table_field">
        <th>Nombre</th>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <span asp-for="ResearcherTypeName">@Html.DisplayFor(modelItem => item.ResearcherTypeName)</span>
                </td>
                <td>
                    <form asp-action="DeleteType">
                        <a asp-action="DeleteType" asp-route-id="@item.ResearcherTypeId" type="submit">Borrar</a>
                    </form>
                </td>
            </tr>
        }

    </table>
</div>
<div>
    <a asp-action="Configurations" asp-controller="Admin">Regresar</a>
</div>

<script>
    function openTheForm() {
        document.getElementById("popupForm").style.display = "block";
    }

    function closeTheForm() {
        document.getElementById("popupForm").style.display = "none";
    }
</script>